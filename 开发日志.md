# 项目开发进展 - OopsOS（对不队）

---

## **项目起步** - 2025年11月13日

### **项目目标：**
- 基于 `xv6` 操作系统的架构，开发一个简化、模块化的操作系统，命名为 **OopsOS**。
- **功能目标：** 提供基本的操作系统功能，如进程管理、内存管理、文件系统和设备驱动等。
- **学习目标：** 通过开发，深入理解操作系统的原理和体系结构。

### **技术选型：**
- **基础框架：** 基于 `xv6` 操作系统，便于扩展与学习。
- **架构支持：** RISC-V 架构，增强系统可移植性和现代硬件支持。
- **编程语言：** C语言（与 `xv6` 保持一致）。

---

## **第一阶段：基础系统架构搭建** - 2025年11月15日

### **开发环境搭建：**
- 安装并配置了 RISC-V 工具链和 QEMU 模拟器，确保能够构建并运行 `xv6`。
- 成功克隆了 `xv6` 源代码，完成编译与测试。
- 配置了本地开发环境，包括使用 IDE（如 VS Code 或 Vim）及调试工具。

### **系统架构设计：**
- 在 `xv6` 基础上设计了初步架构，确保能够运行于 RISC-V 架构，并支持模块扩展。

---

## **第二阶段：模块开发与功能完善**

### **进展与功能：**

- **11/17:**  
  - 实现系统调用 `SYS_cps` ：可查看当前系统的进程信息（包括进程名、PID、优先级等）。

- **11/20:**  
  - 实现系统调用 `SYS_trace` ：能够指定并跟踪系统调用。

- **11/21:**  
  - 实现系统调用 `SYS_sysinfo` ：可查看当前进程数量及剩余空闲内存。
  - **内存管理：** 实现写时分配技术（Copy-On-Write）。

- **11/24:**  
  - **锁机制优化：** 将空闲页面链表和 Buffer Cache 的锁进一步细化，减小锁颗粒度，提高并行效率。

- **11/25:**  
  - **内存管理：** 修改系统调用 `SYS_bark`，并实现懒分配功能。
  - 改写相关用户测试程序。

- **11/26:**  
  - **进程调度：** 修改为动态优先级调度算法：
    - 实现系统调用 `SYS_setPriority` ：支持动态调整进程优先级。
    - 修改进程结构，使其在每次中断时动态调整运行信息。
    - 提高了响应性与公平性，更适应负载变化。

- **11/27:**  
  - 实现系统调用 `SYS_execve` ：执行指定路径的程序。

- **11/31:**  
  - 优化调度算法，修复创建大量子进程可能导致死机的 BUG，提高系统健壮性。

- **12/1:**  
  - 实现系统调用 `SYS_getparentpid` ：获取当前进程的父进程 PID。
  - 实现系统调用 `SYS_print_pgtable` ：打印当前进程的页表信息。

- **12/3:**  
  - **文件管理 & 内存管理：** 实现内存映射文件功能：
    - 支持将文件直接映射到用户空间，比标准 I/O 更高效。
    - 实现系统调用 `SYS_mmap`  和 `SYS_munmap` ，用于建立/取消文件映射。

- **12/4:**  
  - **信号量功能开发：** 实现信号量机制，添加以下系统调用：
    - `SYS_sem_create`: 创建信号量，初始化资源计数。
    - `SYS_sem_free`: 释放信号量，清除资源占用。
    - `SYS_sem_p`: 执行信号量的 P 操作，减少资源计数。
    - `SYS_sem_v`: 执行信号量的 V 操作，增加资源计数。
    - `SYS_sh_var_read`: 读取共享变量的值。
    - `SYS_sh_var_write`: 写入共享变量的值。
  - 完成相关测试代码的编写与调试，修复多次 `fork` 引发的 `sched locks` 问题。
- **12/11:**  
  - **文件管理：** 完善混合索引分配，在原有基础上添加二级间接块
  - 实现符号链接，当一个符号链接打开时，内核跟随该链接指向引用的文件。
    - `SYS_symlink`: 创建符号链接
    - `SYS_create` : 新建一个文件
- **12/12:**
  - **共享内存功能开发：**
    - 实现了共享内存的创建、映射、访问和释放功能，支持进程间共享内存。
    - 添加了以下系统调用：
      - `SYS_shmgetat`: 获取共享内存并将其映射到进程空间。
      - `SYS_shmrefcount`: 查询共享内存的引用计数。
    - 实现了共享内存的引用计数管理、内存分配、映射、解除映射等功能，确保进程间共享内存的同步与资源回收。
    - 完成了父子进程使用共享内存的示例，展示了父进程和子进程如何访问和修改共享内存中的数据。
- **12/17:**
  - **消息队列功能开发：**
    - 实现了消息队列的创建、发送、接收功能,支持进程间通信。
    - 实现了消息队列的创建、发送消息到队列、从队列接收消息的基本功能。
    - 支持消息队列的内存管理，避免了内存碎片。
    - 实现了进程在队列满时的写阻塞和在队列空时的读阻塞机制
    - 添加了以下系统调用：
      - `SYS_mqget`: 获取消息队列。
      - `SYS_msgsnd`: 发送消息到消息队列。
      - `SYS_msgrcv`: 从消息队列接收消息。
- **12/18:**
  - **中断：** 实现`SYS_sigalarm` 和`SYS_sigreturn` 两个系统调用，为用户进程添加定期通知功能，使得进程在一段时间内使用 CPU 后，会被定期“提醒”类似于一种用户态的中断处理，用来模拟用户级的异常处理。
- **12/19:**
  - **网络模块：** 添加e1000网卡驱动，实现对UDP/IP协议的支持
  - 实现系统调用`SYS_connect` ：通过给定的远程地址、本地端口和远程端口来创建一个套接字连接。
- **12/19:**
  - **文件系统：** 实现文件访问控制权限：读、写、执行
  - 实现系统调用`SYS_chmod` ：改变文件的访问权限
- **12/21:**
  - **文件系统：** 实现恢复已删除文件的功能
    - 添加了以下系统调用：
      - `SYS_geti` : 保存文件的索引信息。
      - `SYS_recoveri` : 根据文件的索引信息恢复文件。
- **12/30:**
  - **进程同步/信号量完善：**
    - 允许 `sem_create(0)`，明确 0 初值用于同步屏障与阻塞语义。
    - P/V 使用信号量自身锁进入 `sleep/wakeup`，避免丢唤醒并降低羊群效应。
    - `sem_free` 增加等待者检查，防止释放后等待进程永久挂起。
    - 修复 `wakeup/wakeupOneProc` 锁顺序与重复释放问题（panic: release/acquire）。
    - 新增 `semtest` 并接入 UPROGS，覆盖非法参数、释放后访问、多等待者与并发计数。
    - 新增 AND 接口 `sem_p_multi`，实现多信号量原子申请。
    - 增加 `semandtest` 并纳入 `usertests`，对比“单信号量”与“AND 语义”行为差异。
- **12/31:**
  - **内存管理：** 引入页面换入/换出机制，支持内存紧张时将用户页换出到磁盘并在缺页时换入
    - 新增 swap 盘区与 bitmap 管理，预留连续 swap 区域用于页面换出/回收。
    - 缺页异常路径集成 swapin，兼容懒分配与 COW。
    - 在 `uvmcopy/uvmunmap` 等路径处理 swapped 页的复制与释放。
    - 增加高强度 `swaptest` 并纳入 usertests，覆盖大量内存访问与回读校验。
  - **信号量集功能开发：**
    - 新增 `semset_create/semset_free/semset_p/semset_v/semset_p_multi`，支持信号量集与 AND 原子获取。
    - 增加 `semsettest` 并纳入 `usertests`，覆盖死锁对比与正常通过路径。
  - **演示程序：**
    - 新增 `pcand` 生产者-消费者演示，包含“错误顺序导致死锁”与“AND 原子获取成功”对比。
  - **进程直接通信：**
    - 新增 `dmsgsend/dmsgrcv`，支持基于 PID 的直接消息传递。
    - 每进程维护消息缓冲队列，队列满/空时阻塞发送/接收。
    - 新增 `dmsgtest` 并接入 `usertests`，验证发送/接收与阻塞语义。
    - 新增 `cstest` 客户端-服务器演示，展示请求/响应模型。
    - 修复退出/回收时的锁顺序问题，避免 `panic: acquire`。
    - 补充“接收端退出导致发送阻塞”的测试场景。
    - 修复 `freebytes` 在并发下的锁顺序，避免空闲链表野指针崩溃。
  - **文件系统：**
    - 调整 buffer cache 回收流程（引入 evict_lock，避免跨桶死锁）
    - 补齐 mkfs 二级间接块写入路径
    - 新增 `fallocate` 预分配系统调用（SYS_fallocate=74）：支持指定大小预分配块，支持 FALLOC_KEEP_SIZE 标志；新增 `fallocatetest` 并接入 `usertests`
- **1/1:**
  - **文件系统：**
    - 新增 `fclone` 文件克隆与写时复制（SYS_fclone=75）：共享源文件数据块，写入时触发 COW。
    - 引入块引用计数（brefs），启动时扫描 inode 重建计数，保证克隆写入隔离。
    - 新增 `fclonetest` 验证克隆读写一致性与隔离性，并接入 `usertests`。
  - **管程机制：**
    - 新增 `mon_create/mon_free/mon_enter/mon_exit` 与 `cond_create/cond_free/cond_wait/cond_signal/cond_broadcast`。
    - 采用 Mesa 语义：`cond_wait` 先释放监视器，唤醒后重新竞争进入。
    - 新增 `monitortest` 并接入 `usertests`。
    - 修复进程异常退出时持有监视器导致的永久锁死问题。
  - **线程机制完善：**
    - `clone` 增加用户栈页对齐校验与 guard page 处理。
    - 线程入口参数通过 `a0` 传递，修正参数传递逻辑。
    - 新增 `gettid/gettgid/thread_exit`，区分线程与线程组并规范退出语义。
    - 更新 `uthread` 用户库：统一入口 `thread_entry`，线程退出自动回收。
    - 新增 `threadtest` 并接入 `usertests`。
  - **文件系统：** 新增 `lseek` 文件偏移量随机访问系统调用（SYS_lseek=76）
    - 实现 `sys_lseek`：支持随机访问并修改文件偏移。
    - 支持 `SEEK_SET/SEEK_CUR/SEEK_END` 三种偏移模式。
    - 在 `fcntl.h` 定义 SEEK 常量。
    - 新增 `lseektest` 覆盖基本功能、随机访问与边界条件，并接入 `usertests`。
  - **进程调度：**
    - 引入多级反馈队列（MLFQ），按队列区分时间片并在耗尽后降级。
    - 周期性提升全体进程，避免长期饥饿。
    - 唤醒时提升至高优先级队列，保证交互任务及时响应。
    - 增加 SCHED 编译开关，支持 MLFQ/DYNPRIO 对比构建。

- **1/2:**
  - **文件系统：** 新增 `truncate/ftruncate` 系统调用（SYS_truncate=77, SYS_ftruncate=78），支持按路径或文件描述符截断/扩展文件大小；新增 `truncatetest` 测试用例并接入 `usertests`。
  - **文件系统：** 新增 `rename` 系统调用（SYS_rename=79），支持对普通文件/设备文件/符号链接进行重命名与覆盖；新增 `renametest` 覆盖基本重命名、覆盖与跨目录移动场景并接入 `usertests`。
  - **文件系统：** 新增 `dedup` 块级在线去重系统调用（SYS_dedup=80），将 dst 与 src 的相同数据块合并为共享块，并复用块引用计数 + COW 保证写入隔离；新增 `deduptest` 并接入 `usertests`；新增 `dedup` 用户命令便于演示。
  - **文件系统：** 扩展 `fallocate` 支持稀疏文件打洞（FALLOC_FL_PUNCH_HOLE），可释放文件中间任意范围的块而保持文件大小不变；新增 `ipunch` 内核函数处理块释放与间接块回收；更新接口为 `fallocate(fd, offset, len, flags)`；修复稀疏文件读取路径使用 `bmap_nalloc()` 避免事务外块分配；新增 `punchtest` 并接入 `usertests`。

  - **进程调度：** 更新 `cps` 输出字段，展示 MLFQ 层级与时间片使用情况；新增 `schedtest` 对比测试，量化交互型任务延迟。
  - **内存管理：** 实现 ELF 按需加载
    - `exec` 为可加载段建立 VMA，避免一次性分配全部物理页
    - 缺页时从文件加载并映射
    - 新增 `demandloadtest` / `bigbss` 并接入 `usertests`
  - **实时调度（LLF）：**
    - 新增 `rt_set/rt_clear` 系统调用，支持 LLF 实时调度类与普通调度共存
    - 进程结构增加 `rt_*` 字段，按 period/runtime/deadline 维护预算与截止期
    - 调度器优先选择 LLF 任务，普通调度跳过 RT 任务
    - 时钟中断更新预算，支持更小松弛度任务抢占
    - 新增 `llftest`
  - **优先级继承（PI）：**
    - 为 `monitor/sleeplock` 增加优先级捐赠与释放回收，缓解优先级倒置
    - DYNPRIO 使用继承优先级作为动态优先级基准，MLFQ 捐赠提升至最高队列
    - 新增 `pitest` 手动测试，验证高优先级可先于中优先级拿到锁
  - **死锁检测与测试完善：**
    - 在 `sem_p/semset_p/mon_enter` 阻塞前进行死锁检测，发现等待环路返回 `-1`
    - 仅在 `resource_count == 0` 时记录 `owner`，避免被等待者覆盖
    - 新增 `deadlocktest`，覆盖信号量/信号量集/监视器的检测与恢复路径
    - `usertests` 运行时仅输出 OK/FAILED，单独运行测试保留完整输出
- **1/3:**
  - **设备管理：** 新增 spooling 字符设备（`SPOOL`），以环形缓冲区实现阻塞读写，生产者与输出设备解耦。
  - **测试：** 新增 `spooltest` 并接入 `usertests`，验证 spooling 读写与输出一致性。
  - **文档：** 新增 `设备管理.md`，完善设备管理总览并保留网络设备章节；
    修复 `网络设备.md` 乱码，恢复为完整内容版本并保留图片与测试说明。
  - **测试输出：** `spooltest` 独立运行输出详细过程，在 `usertests` 中使用安静参数只保留 OK/FAILED。
  - **磁盘缓冲管理：** buffer cache 引入 read-ahead + Clock（二次机会）置换策略，顺序读场景预取下一块并使用 `refbit` 进行回收；新增 `readaheadtest` 并接入 `usertests`（安静模式）。
  - **磁盘存储管理：** `balloc` 增加块分配 hint，从最近成功分配位置开始扫描并回绕；新增 `ballochinttest` 并接入 `usertests`（安静模式）。
  - **测试调优：** 缩小 `ballochinttest` 文件/块规模，避免全量回归时触发 inode 枯竭。  
  - **内存管理：**
    - 置换策略升级为标准 Clock（二次机会）扫描，两轮内优先淘汰 A 位长期未置位页。
    - 新增 `swapperftest` 性能压力测试（默认不纳入 usertests，避免回归过慢）。
    - 支持 `MAP_SHARED` 文件映射页参与置换：淘汰时写回文件并回收物理页。
    - 新增 `mmapswaptest` 并接入 usertests，验证文件映射页在内存压力下的一致性。
    - 并发优化 swap：位图分配引入 `swap_hint`，进程级 `swap_hand` 降低并发换出时的锁竞争。
    - 新增 `swapconctest` 并接入 usertests，展示多进程并发换入/换出的效率与正确性。
    - 抖动预防：引入 swap 事件计数窗口与轻量节流，频繁换入/换出时主动短暂让出 CPU。
    - 新增 `swapthrashtest` 并接入 usertests，验证高压力下的稳定性与节流效果。
    - 调整 `swapthrashtest` 默认压力与超时检查频率，避免全量回归长时间卡住。
    - `swapthrashtest` 增加进度输出，仅由主进程打印并降低默认并发与时长。
    - 轻量页面缓冲（PBA lite）：缓存最近换出页，命中时跳过 swap 读盘。
    - 新增 `swapbuftest` 并接入 usertests，验证 swap buffer 命中统计。
    - 新增 `swapbufcmptest` 对照测试，比较顺序读/逆序读下 swap buffer 命中与耗时（默认不纳入 usertests）。
    - 调整 `swapconctest`：仅主进程打印进度，降低默认压力并缩短超时阈值，避免全量回归卡住。
  - **内存管理——测试输出控制：**
    - `swapconctest` 增加 `verbose` 参数，支持多进程打印演示，默认仍只主进程输出以保持回归稳定。（`verbose=1` 时所有进程都会打印；默认不传就是只主进程打印。）
  - **回归测试调整：**
    - 将 `swapthrashtest` 从 `usertests` 中移除，避免全量回归耗时过长。- **1/3:**
  - **文件系统：** 新增 `flock` 文件锁系统调用（SYS_flock=87），支持共享锁（LOCK_SH）和排他锁（LOCK_EX）实现多进程文件访问协调；支持非阻塞模式（LOCK_NB）；在 inode 中维护锁类型和持有者计数；新增 `flocktest` 覆盖共享锁/排他锁/阻塞等待/冲突检测场景并接入 `usertests`。
  - **文件系统：** 新增 `fsync/fdatasync` 文件同步系统调用（SYS_fsync=88, SYS_fdatasync=89），确保文件数据持久化到磁盘；fsync 同步数据+元数据，fdatasync 仅同步数据；与日志层集成触发事务提交；新增 `fsynctest` 并接入 `usertests`。
  - **文件系统：** 新增 `setxattr/getxattr/listxattr/removexattr` 文件扩展属性系统调用（SYS_setxattr=90~93），支持为文件附加任意 key-value 元数据；每文件最多 8 个属性，属性名最长 64 字节，值最长 256 字节；新增 `xattrtest` 并接入 `usertests`。
  - **实时调度（LLF）：**
    - 新增 `rt_set/rt_clear` 系统调用，支持 LLF 实时调度类与普通调度共存
    - 进程结构增加 `rt_*` 字段，按 period/runtime/deadline 维护预算与截止期
    - 调度器优先选择 LLF 任务，普通调度跳过 RT 任务
    - 时钟中断更新预算，支持更小松弛度任务抢占
    - 新增 `llftest`
  - **优先级继承（PI）：**
    - 为 `monitor/sleeplock` 增加优先级捐赠与释放回收，缓解优先级倒置
    - DYNPRIO 使用继承优先级作为动态优先级基准，MLFQ 捐赠提升至最高队列
    - 新增 `pitest` 手动测试，验证高优先级可先于中优先级拿到锁
  - **死锁检测与测试完善：**
    - 在 `sem_p/semset_p/mon_enter` 阻塞前进行死锁检测，发现等待环路返回 `-1`
    - 仅在 `resource_count == 0` 时记录 `owner`，避免被等待者覆盖
    - 新增 `deadlocktest`，覆盖信号量/信号量集/监视器的检测与恢复路径
    - `usertests` 运行时仅输出 OK/FAILED，单独运行测试保留完整输出
  - **银行家算法（死锁避免）：**
    - 新增 `banker_init/banker_set_max/banker_request/banker_release` 系统调用
    - 全局维护 `total/available` 与进程级 `max/alloc`，请求前进行安全性检查
    - 进程退出时回收持有资源，避免资源泄露导致误判
    - 新增 `bankertest`，覆盖安全/不安全请求的对比验证
  - **中级调度（进程级挂起/恢复）：**
    - 新增 `SUSPENDED/SUSPENDED_SLEEP` 状态，挂起后从就绪队列移除
    - 内存压力时选择候选进程并批量换出用户页（`swapout_proc`）
    - 空闲页回升后自动恢复挂起进程
    - `sysinfo` 增加 `nsuspended` 统计
    - 新增 `SYS_midsched` 开关，关闭时恢复所有挂起进程
    - 新增 `midschedtest` 
    - 新增 `midschedcmp` 对比测试（手动运行）
